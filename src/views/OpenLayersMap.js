import React, { useEffect, useRef } from "react";
import MapComponent from "./Catalog/Map/MapComponent";

const OpenLayersMap = () => {
  return (
    <>
      <MapComponent />
    </>
  );
};

export default OpenLayersMap;
